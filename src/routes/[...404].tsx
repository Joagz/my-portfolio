import { A } from "@solidjs/router"; // (<a></a> tag)
import Layout from "~/components/Layout";

export default function NotFound() {
  return (
    <Layout>
      <h1>Not found :(</h1>
    </Layout>
  );
}
